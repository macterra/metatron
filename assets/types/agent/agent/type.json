{
    "name": "agent",
    "description": "An agent is an asset that can own another asset",
    "schema": "schema",
    "json": "agent.json",
    "html": "index.html",
    "template": "template.html",
    "update": "docker run --rm -v $PWD:/app/io macterra/metatron-agent"
}
